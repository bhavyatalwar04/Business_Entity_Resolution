"""Write matching_results.tsv and candidate_pairs.tsv, then run the official validator."""


def write_outputs(matches, candidates, test_s1, out_dir="output"):
    """One row per test S1 (empty lists allowed), comma-joined IDs, then validate."""
    raise NotImplementedError
